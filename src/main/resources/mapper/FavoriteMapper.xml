<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.home.favorite.FavoriteMapper">
    <!-- 로그인한 회원의 관심지역을 저장합니다. member_id가 소유권 기준입니다. -->
    <insert id="insert" parameterType="com.ssafy.home.favorite.Favorite" useGeneratedKeys="true" keyProperty="id">
        insert into favorites (member_id, sido_nm, sigungu_nm, dong_nm, lawd_cd, memo)
        values (#{memberId}, #{sidoNm}, #{sigunguNm}, #{dongNm}, #{lawdCd}, #{memo})
    </insert>

    <!-- 관심지역 페이지에서 현재 회원이 저장한 지역만 최신순으로 조회합니다. -->
    <select id="findByMemberId" resultType="com.ssafy.home.favorite.Favorite">
        select * from favorites where member_id = #{memberId} order by id desc
    </select>

    <!-- id와 member_id를 함께 조건으로 사용해 다른 회원의 관심지역을 삭제하지 못하게 합니다. -->
    <delete id="delete">
        delete from favorites where id = #{id} and member_id = #{memberId}
    </delete>
</mapper>
